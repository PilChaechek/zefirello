## 📋 Этап 3: Управление Досками (Boards Feature)
*Фича: "Я хочу создавать пространства (доски) для работы".*
1.  **Database:** Таблица `boards` (user_id, name, color).
2.  **Backend:**
    *   `BoardController` (CRUD).
    *   `CreateBoardAction` (Logic).
    *   `BoardResource`.
3.  **Frontend:**
    *   Dashboard Page (Список досок).
    *   Modal: "Create Board" (Dialog component).
    *   Routing: Переход внутрь доски (`/boards/{id}`).

## Подробные шаги
### 1. Подключаем общий Layout + компонент Sidebar
```bash
npx shadcn-vue@latest add sidebar
```

### 2. DarkMode по умолчанию + переключатель в шапке
#### 2.1 Ставим `VueUse`
- [VueUse]() -  сама управляет классом dark на теге html и сохраняет выбор пользователя в localStorage
```bash
npm install @vueuse/core
```

#### 2.2 Копируем компонент `dropdown-menu`
```bash
npx shadcn-vue@latest add dropdown-menu
```

## Фичи:
- 14.12 Вынес админа в .env, убрал везде где светился пароль, поправил сидер создания админа
- 
